# Copy file này thành .env và điền đúng giá trị

# Port backend Express của bạn đang chạy (mặc định 3000)
VITE_BACKEND_URL=http://localhost:3000

# Prefix cho axios — giữ nguyên /api, Vite proxy sẽ tự forward
VITE_API_URL=/api
